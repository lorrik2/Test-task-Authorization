import { User } from '../features/auth/types/User';
import Data from '../server/artificialBase.json';

//export const login = (obj: User): Promise<User> => {
//  const res = new Promise((resolve, reject) => {
//    setTimeout(() => {
//      console.log(Data);
//    }, 100);
//  });
//  return res.then((data) => console.log(data)).catch((error) => crossOriginIsolated.log(error));
//};
